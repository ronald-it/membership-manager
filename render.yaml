services:
  - type: web
    name: membership-manager
    env: docker
    dockerfilePath: ./Dockerfile
    buildCommand: ""
    startCommand: php-fpm
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: false
      - key: LOG_CHANNEL
        value: stderr
